// Reuse the Open Graph image for the Twitter/X large-image card so both
// previews stay identical and there is a single source of truth.
export { default, alt, size, contentType } from "./opengraph-image";
